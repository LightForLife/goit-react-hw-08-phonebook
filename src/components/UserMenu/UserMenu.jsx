export const UserMenu = () => {
  return (
    <div>
      <p>mango@mail.com</p>
      <button type="button" onClick={() => console.log('Click logout')}>
        Logout
      </button>
    </div>
  );
};
